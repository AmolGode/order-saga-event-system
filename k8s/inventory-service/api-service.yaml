apiVersion: v1
kind: Service
metadata:
  name: inventory-service-api
spec:
  type: NodePort
  selector:
    app: inventory-service-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30200 # mirrors the 8200 host port used in docker-compose
