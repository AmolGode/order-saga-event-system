apiVersion: v1
kind: Service
metadata:
  name: inventory-db
spec:
  clusterIP: None # headless — standard pairing for a StatefulSet, gives stable pod DNS
  selector:
    app: inventory-db
  ports:
    - port: 5432
      targetPort: 5432
