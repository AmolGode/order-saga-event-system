apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service-worker
  template:
    metadata:
      labels:
        app: inventory-service-worker
    spec:
      containers:
        - name: inventory-service-worker
          image: order-saga-event-system-inventory-service-worker:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9100
          envFrom:
            - secretRef:
                name: inventory-service-worker-secret
          env:
            - name: POSTGRES_HOST
              value: inventory-db
            - name: KAFKA_BROKERS
              value: kafka-broker:9092
            - name: KAFKA_GROUP_ID
              value: inventory-service-worker
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /metrics
              port: 9100
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /metrics
              port: 9100
            initialDelaySeconds: 10
            periodSeconds: 15
