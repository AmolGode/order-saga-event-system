apiVersion: v1
kind: Service
metadata:
  name: analytics-service-api
spec:
  type: NodePort
  selector:
    app: analytics-service-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30400 # mirrors the 8400 host port used in docker-compose
